[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apeSketch"
version = "0.1.0"
description = "Hand-ink bridge for ape* structural workflows: capture, session, agent export."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Nicolas Mora Bowen", email = "nmorabowen@gmail.com" },
]
license = "MIT"
license-files = ["LICENSE"]
keywords = ["ink", "sketch", "stylus", "whiteboard", "agent", "scratchpad"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering",
    "Typing :: Typed",
]

dependencies = [
    "websockets>=12",
    "segno>=1.6",
]

[project.optional-dependencies]
dev = [
    "pyright>=1.1.360",
    "ruff>=0.5",
    "pytest>=8",
    "pytest-cov>=5",
]
# Kept as an alias: host extras are now the default install.
host = [
    "websockets>=12",
    "segno>=1.6",
]

[project.scripts]
apeSketch = "apeSketch.host.server:main"

[project.urls]
Homepage = "https://github.com/nmorabowen/apeSketch"
Repository = "https://github.com/nmorabowen/apeSketch"
Issues = "https://github.com/nmorabowen/apeSketch/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"apeSketch" = ["py.typed"]
"apeSketch.host" = ["static/*"]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = [
    "--strict-markers",
    "--strict-config",
    "-ra",
]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pyright]
include = ["src"]
pythonVersion = "3.11"
typeCheckingMode = "strict"
